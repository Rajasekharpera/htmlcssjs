import  React from 'react';
import  ReactDOM  from 'react-dom';
import { render } from 'react-dom';
    
const Test =(props)=>{
    return(
        <p>this is me{props.name}</p>
    );
}
export default Test;
